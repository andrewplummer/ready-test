'use strict';

fdescribe('Focused suite', function() {

  it('should assert inside focused suite', function() {
    assert(true);
  });

});

fpdescribe('Focused perf suite', function() {

  it('should assert inside focused perf suite', function() {
    assert(true);
  });

});
