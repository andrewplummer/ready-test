'use strict';

describe('Should be focused', function() {

  fit('Should assert inside focused test', function() {
    assert(true);
  });

  fpit('Should assert inside focused perf test', function() {
    assert(true);
  });

});
