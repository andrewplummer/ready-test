'use strict';

fdescribe('Should be focused', function() {

  it('Should assert inside focused suite', function() {
    assert(true);
  });

});

fpdescribe('Should be focused', function() {

  it('Should assert inside focused perf suite', function() {
    assert(true);
  });

});
